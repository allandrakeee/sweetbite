<?php
/**
 * Theme constant variables
 * 
 * @package sweetbite
 * @subpackage Theme Variables
 * @author Solomon Culaste
 */

// $dev_domains = array('localhost', 'wordpress.dev');
// foreach($dev_domains as $dev_domain) {
//     if(strpos($_SERVER['SERVER_NAME'], $dev_domain)) DEFINE('ENV', 'local'); break;
// }
DEFINE('ENV', 'local');